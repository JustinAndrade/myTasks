const initialState = {
  goals: [
    {
      name: "Finish School",
    },
  ],
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

module.exports = reducer;
